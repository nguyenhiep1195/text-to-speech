# --- Core (always required) ---
edge-tts>=6.1.9

# --- Optional: Google TTS engine ---
# pip install gTTS
gTTS>=2.5.1

# --- Optional: Kokoro offline engine ---
# pip install kokoro soundfile
# kokoro>=0.9.2
# soundfile>=0.12.1
