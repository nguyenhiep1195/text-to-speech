# --- Core (always required) ---
edge-tts>=6.1.9

# --- AI SSML generation ---
google-genai>=1.0.0          # Gemini (free tier) — recommended
openai>=1.0.0                # ChatGPT (paid) — optional
python-dotenv>=1.0.0

# --- Optional: Google TTS engine ---
# pip install gTTS
gTTS>=2.5.1
